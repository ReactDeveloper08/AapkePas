export const isGetHome = state => {
  return state.docHome.getDocHome;
};

export const isGoLive = state => {
  return state.docHome.GoLive;
};

export const isGetOnline = state => {
  return state.docHome.getOnline;
};
