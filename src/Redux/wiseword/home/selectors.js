export const isGetHome = state => {
  return state.home.getHome;
};

export const isGetCategories = state => {
  return state.home.getCategories;
};

export const isGetNotification = state => {
  return state.home.getNotification;
};

export const isExpertSelect = state => {
  return state.home.getExpertList;
};
