export const isTerms_condition = state => {
  return state.policies.getTerms_Condition;
};

export const isPrivacy_Policies = state => {
  return state.policies.getPrivacy_Policies;
};

export const isContactUs = state => {
  return state.policies.getContactUs;
};

export const isFaqQuestion = state => {
  return state.policies.getFaqQuestion;
};

export const isFaqCategories = state => {
  return state.policies.getFaqCategories;
};
